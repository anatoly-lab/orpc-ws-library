{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Explicit `types` — Node-only package. TS 6.0 defaults `types` to "all
    // visible @types/*"; pinning it to ["node"] keeps the global type surface
    // deterministic (and documents that `fetch`/`Response`/`URL` come from
    // @types/node, see the no-DOM note below). Mirrors @orpc-ws/shared.
    "types": ["node"],
    "rootDir": "./src",
    "outDir": "./dist",
    // Buildinfo at the package root, NOT inside `dist`: the typecheck pass
    // (`tsc -p tsconfig.json --noEmit`, incremental) writes this file, and
    // `files` ships `dist`, so `./dist/.tsbuildinfo` would get packed.
    // Root `.tsbuildinfo` is gitignored and outside `files`.
    "tsBuildInfoFile": "./.tsbuildinfo"
    // No DOM lib — this is a Node-only package. `fetch`, `Response`,
    // `URL` come from @types/node 18+. Browser-side counterpart is
    // `@orpc-ws/oidc-pkce`, which adds DOM here.
  },
  "include": ["src/**/*.ts"],
  "exclude": ["dist", "node_modules", "**/*.test.ts", "**/__tests__/**"]
}
