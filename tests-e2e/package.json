{
  "name": "@repo/tests-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright end-to-end tests driving the cookie-BFF demo SPA + demo server + real Keycloak (testcontainers). Exercises the full server-side OIDC → httpOnly session cookie → WS → ORPC flow no single-package test can cover.",
  "type": "module",
  "scripts": {
    "test": "node -e \"console.log('[tests-e2e] skipped — run pnpm --filter @repo/tests-e2e test:e2e to run Playwright (requires Docker)')\"",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:list": "playwright test --list",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint .",
    "build": "tsc -p tsconfig.json --noEmit",
    "clean": "rimraf .turbo playwright-report test-results *.tsbuildinfo"
  },
  "devDependencies": {
    "@playwright/test": "1.61.0",
    "@types/node": "26.0.0",
    "rimraf": "6.1.3",
    "testcontainers": "12.0.3",
    "tsx": "4.22.4",
    "typescript": "6.0.3"
  }
}
